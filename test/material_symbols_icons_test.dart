// ignore_for_file: unused_local_variable

import 'package:flutter_test/flutter_test.dart';

import 'package:material_symbols_icons/symbols.dart';

void main() {
  // We just try to reference a example IconData from each of the classes which should
  // have been generated.
  const materialUnivOutlined = Symbols.abc;
  const materialUnivRounded = Symbols.abc_rounded;
  const materialUnivSharp = Symbols.abc_sharp;
  test('adds one to input values', () {});
}
